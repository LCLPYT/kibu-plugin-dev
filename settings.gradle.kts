pluginManagement {
    repositories {
        maven {
            name = "LCLPNetwork"
            url = uri("https://repo.lclpnet.work/repository/internal")
        }
        gradlePluginPortal()
    }
}

rootProject.name = "kibu-plugin-dev"
